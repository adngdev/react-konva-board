import React from 'react';

const Toolbar = () => {
    return (
        <div className={`w-full z-20 h-14 bg-zinc-100 border-b border-zinc-300`}>
            <div className={`pl-32 h-full flex items-center`}>
                <button type={`button`} onClick={() => {}}>
                    Flip
                </button>
            </div>
            asdasd
        </div>
    );
};

export default Toolbar;
